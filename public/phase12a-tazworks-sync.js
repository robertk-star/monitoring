(function () {
  function text(el){return (el&&el.textContent?el.textContent:'').trim();}
  function isSettings(){return Array.from(document.querySelectorAll('.page-header h1')).some(h=>text(h)==='Settings');}
  function esc(v){return String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'","&#039;");}
  function date(v){if(!v)return '';const d=new Date(v);return Number.isNaN(d.getTime())?String(v):d.toLocaleString();}
  async function api(path,opt){const r=await fetch('/api/index?path='+encodeURIComponent(path),Object.assign({credentials:'include'},opt||{},{headers:Object.assign({'Content-Type':'application/json'},(opt&&opt.headers)||{})}));const raw=await r.text();let data={};try{data=raw?JSON.parse(raw):{}}catch{}if(!r.ok)throw new Error(data.message||'TazWorks sync failed.');return data;}
  function toast(m,d){let b=document.getElementById('phase12a-toast');if(!b){b=document.createElement('div');b.id='phase12a-toast';document.body.appendChild(b)}b.className=d?'phase12a-toast danger':'phase12a-toast';b.textContent=m;clearTimeout(b.t);b.t=setTimeout(()=>b.remove(),6500)}
  function summary(s){const box=document.getElementById('phase12a-summary');if(box)box.innerHTML='<h3>Latest raw sync summary</h3><pre>'+esc(JSON.stringify(s||{},null,2).slice(0,14000))+'</pre>'}
  function render(runs){const body=document.querySelector('#phase12a-runs tbody');if(!body)return;if(!runs||!runs.length){body.innerHTML='<tr><td colspan="8" class="phase12a-empty">No sync runs yet.</td></tr>';return}body.innerHTML=runs.map(run=>`<tr><td>${esc(date(run.started_at))}</td><td>${esc(run.status||'')}</td><td>${esc(run.orders_pulled??0)}</td><td>${esc(run.applicants_upserted??0)}</td><td>${esc((run.raw_summary&&run.raw_summary.medExpireUpdated)||0)}</td><td>${esc(run.errors_count??0)}</td><td>${esc(run.message||'')}</td><td><button data-sum='${esc(JSON.stringify(run.raw_summary||{}))}'>Summary</button></td></tr>`).join('')}
  async function load(){try{const d=await api('tazworks-sync/runs');render(d.runs||[]);if(d.runs&&d.runs[0])summary(d.runs[0].raw_summary||{})}catch(e){toast(e.message||'Could not load sync runs.',true)}}
  async function run(){const b=document.getElementById('phase12a-run-sync');if(b){b.disabled=true;b.textContent='Running Sync...'}try{const d=await api('tazworks-sync/run',{method:'POST',body:'{}'});toast(d.message||'Sync complete.');await load();}catch(e){toast(e.message||'TazWorks sync failed.',true);await load()}finally{if(b){b.disabled=false;b.textContent='Run TazWorks Sync Now'}}}
  function panel(){if(!isSettings()||document.getElementById('phase12a-panel'))return;const anchor=Array.from(document.querySelectorAll('section.card')).find(s=>text(s).includes('System Check'));const p=document.createElement('section');p.id='phase12a-panel';p.className='card wide-card settings-card phase12a-panel';p.innerHTML=`<h2>TazWorks Manual Sync</h2><p class="muted">Pulls recent orders, scans MVR/editor results, and updates Med Expire only from the Medical Certificate expiration date.</p><div class="phase12a-warning">Phase 12A-19: clears stale Med Expire when no medical info exists.</div><div class="phase12a-actions"><button id="phase12a-run-sync" type="button" class="primary-inline">Run TazWorks Sync Now</button><button id="phase12a-refresh-runs" type="button">Refresh Sync Log</button></div><div class="phase12a-table-wrap"><table id="phase12a-runs"><thead><tr><th>Started</th><th>Status</th><th>Orders</th><th>Monitoring</th><th>Med Dates</th><th>Errors</th><th>Message</th><th></th></tr></thead><tbody><tr><td colspan="8" class="phase12a-empty">Loading sync runs...</td></tr></tbody></table></div><div id="phase12a-summary" class="phase12a-summary"></div>`;(anchor||document.querySelector('.main-panel')).insertAdjacentElement(anchor?'afterend':'beforeend',p);document.getElementById('phase12a-run-sync')?.addEventListener('click',run);document.getElementById('phase12a-refresh-runs')?.addEventListener('click',load);load()}
  function styles(){if(document.getElementById('phase12a-style'))return;const st=document.createElement('style');st.id='phase12a-style';st.textContent='.phase12a-panel{border-left:5px solid #16a34a}.phase12a-warning{background:#ecfdf5;border:1px solid #bbf7d0;color:#166534;border-radius:12px;padding:10px 12px;margin:10px 0 14px;font-weight:700}.phase12a-actions{display:flex;gap:10px;margin-bottom:14px}.phase12a-actions button,#phase12a-runs button{border:1px solid #16a34a;background:#f0fdf4;color:#166534;border-radius:999px;padding:7px 10px;font-size:12px;font-weight:900}.phase12a-table-wrap{overflow:auto;border:1px solid #e5e7eb;border-radius:14px}#phase12a-runs{width:100%;border-collapse:collapse}#phase12a-runs th,#phase12a-runs td{padding:9px 10px;border-bottom:1px solid #e5e7eb;font-size:13px;vertical-align:top}.phase12a-summary pre{max-height:360px;overflow:auto;background:#0f172a;color:#e2e8f0;padding:12px;border-radius:12px;font-size:12px}.phase12a-toast{position:fixed;right:18px;bottom:18px;z-index:10030;background:#111827;color:#fff;border-radius:12px;padding:12px 14px;box-shadow:0 18px 45px rgba(15,23,42,.25);font-size:14px;max-width:460px}.phase12a-toast.danger{background:#991b1b}';document.head.appendChild(st)}
  document.addEventListener('click',e=>{const b=e.target&&e.target.closest?e.target.closest('[data-sum]'):null;if(b){try{summary(JSON.parse(b.dataset.sum||'{}'))}catch{summary({})}}});
  function refresh(){styles();panel()}setInterval(refresh,1200);if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',refresh);else refresh();
})();

// PHASE12A20_SETTINGS_ONLY_TAIL
(function () {
  function text(el) { return (el && el.textContent ? el.textContent : '').trim(); }
  function isSettings() { return Array.from(document.querySelectorAll('.page-header h1')).some((h) => text(h) === 'Settings'); }
  function cleanup() {
    if (isSettings()) return;
    document.getElementById('phase12a-panel')?.remove();
  }
  setInterval(cleanup, 500);
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', cleanup);
  else cleanup();
})();
